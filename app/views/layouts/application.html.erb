<!DOCTYPE html>
<html>
  <head>
    <title>CFapp</title>

    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/js/bootstrap.min.js"></script>
    <%= csrf_meta_tags %>

  <meta name="viewport" content="width=device-width, initial-scale=1">
  <%= csrf_meta_tags %>
  </head>

  <body>
    <% if notice %>
      <p id="notice" class="alert alert-info"><%= notice %></p>
    <% end %>
    <% if alert %>
      <p id="alert" class="alert alert-info" role="alert"><%= alert %></p>
    <% end %>
    
    <nav>
    <ul>
      <li class= menu-links> <%= link_to "Home", static_pages_landing_page_path %> </li>
      <li class= menu-links> <%= link_to "About", static_pages_about_path %> </li>
      <li class= menu-links> <%= link_to "Products", products_url %> </li>
      <li class= menu-links> <%= link_to "Contact", static_pages_contact_path %> </li>
      <li class= menu-links> <%= form_tag("/products", method: "get") do %>
          <%= label_tag(:q, "Search for:") %>
          <%= text_field_tag(:q) %><br>
          <br>
          <%= submit_tag("Search", class: "btn btn-default")%>
          <% end %>
    </ul>
    <ul class="nav pull-right">
      <li>
      <% if user_signed_in? %>
        <%= link_to('Logout', destroy_user_session_path, :method => :delete) %>
      <% else %>
        <%= link_to('Login', new_user_session_path) %>
      <% end %>
      </li>
    </ul>

    </nav>
  
   
   
 
  <%= yield %>

  <footer>
    &copy; 2017 Bike Berlin
  </footer>

  </body>
</html>
